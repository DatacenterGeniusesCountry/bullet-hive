{
  "name": "bullet-hive",
  "version": "1.0.0",
  "description": "Knowledge sharing server for ACE system - shared bullet storage and sync",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply bullet-hive-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply bullet-hive-db --remote"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.6",
    "hono": "^4.12.3",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260305.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.69.0"
  }
}
